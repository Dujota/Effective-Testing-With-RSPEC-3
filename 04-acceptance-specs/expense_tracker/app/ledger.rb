module ExpenseTracker

RecordResult = Struct.new(:success?, :expense_id, :error_message)

  class Ledger

    def record(expense)
      #code
    end

    def expenses_on(date)
      #code
    end

  end
end
